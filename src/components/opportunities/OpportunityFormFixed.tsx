"use client"

// This file was previously corrupted. To avoid typecheck/build failures while
// preserving any legacy imports, we re-export the new tabbed form.
"use client"

// Legacy shim: re-export the new tabbed form to preserve existing imports
export { default } from './OpportunityFormTabbed'